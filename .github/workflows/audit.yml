name: v91 Audit Pipeline

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  audit-checks:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install Dependencies
        run: |
          pip install --no-cache-dir numpy pytest

      - name: Run Unit Tests
        run: |
          pytest tests/ -v

      - name: Initialise Listener DB for integration tests
        run: |
          mkdir -p data
          sqlite3 data/v91_independent_state.db < data/init_listener_db.sql
          echo "DB initialised"

      - name: Run Listener Integration Tests
        run: |
          pytest tests/test_listener.py -v

      - name: Hash Verification
        run: |
          find ./controller -type f -name "*.py" -exec sha256sum {} \; > audit_hashes.txt
          sha256sum audit_hashes.txt > audit_hashes.sha256
          echo "Hash verification complete"
          cat audit_hashes.txt

      - name: Upload Hash Artifacts
        uses: actions/upload-artifact@v3
        with:
          name: audit-hashes
          path: |
            audit_hashes.txt
            audit_hashes.sha256
